"""Control wibble built-in speaker (V2 only)."""

def off() -> None:
    """Turn wibble speaker off.

Example: ``speaker.off()``

This does not disable sound output to an edge connector pin."""
    ...

def on() -> None:
    """Turn wibble speaker on.

Example: ``speaker.on()``"""
    ...